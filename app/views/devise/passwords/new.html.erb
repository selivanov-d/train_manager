<% @page_title = t '.title' %>

<% content_for :sidebar do %>
    <% if @user.errors.any? %>
        <%= render 'common/errors', resource: @user %>
    <% end %>
<% end %>

<div class="password-new">
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'password-new-form' }) do |f| %>
      <div class="password-new-form_input-row">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, class: 'password-new-form_input' %>
      </div>

      <div class="password-new-form_actions">
        <%= f.submit t('.send_me_instructions'), class: 'password-new-form_submit-button' %>
      </div>
  <% end %>

  <%= render 'devise/shared/links' %>
</div>
