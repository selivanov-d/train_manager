<% @page_title = t '.title', route_name: @route.name %>

<div class="admin-route-edit">
  <%= render 'form' %>

  <div class="admin-route-edit_actions">
    <%= link_to t('common.return_to_routes'), admin_routes_path, class: 'admin-route-edit_back-button' %>
  </div>
</div>
