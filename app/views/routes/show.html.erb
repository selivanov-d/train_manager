<p id="notice"><%= notice %></p>

<p>
  <strong>Название:</strong>
  <%= @route.name %>
</p>

<p>
  <strong>Поезда на этом маршруте:</strong>
  <% if @route.trains.present? %>
    <ul>
      <%= render partial: 'trains/train_as_list_item', collection: @route.trains, as: :train %>
    </ul>
<% else %>
    Поездов на маршруте нет
<% end %>
</p>

<p>
  <strong>Станции этого маршрута:</strong>
  <% if @route.railway_stations.present? %>
    <ul>
      <%= render partial: 'railway_stations/railway_station_as_list_item',
          collection: @route.railway_stations.ordered,
          as: :railway_station %>
    </ul>
<% else %>
    Станций в маршруте нет
<% end %>
</p>

<%= link_to 'Редактировать', edit_route_path(@route) %> |
<%= link_to 'К списку маршрутов', routes_path %>
